name: Main workflow
on:
  push:
    paths-ignore:
      - "**.md"
  pull_request:
    paths-ignore:
      - "**.md"

jobs:
  # Run the `rustfmt` code formatter
  rustfmt:
    name: Rustfmt [Formatter]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          components: rustfmt
          override: true
      - run: rustup component add rustfmt
      - uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: --all -- --check

  # Run the `clippy` linting tool
  clippy:
    name: Clippy [Linter]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: rustup component add clippy
      - uses: actions-rs/clippy-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: --all-targets --all-features -- -D clippy::all

  # Run a security audit on dependencies
  audit:
    name: Cargo Audit [Security]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/audit-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  # Ensure that the project could be successfully compiled
  check:
    name: Cargo Check [Compile]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: check

    # Ensure that the project could be successfully compiled
  test:
    name: Cargo Test [Test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/cargo@v1
        with:
          command: test

  # Create GitHub release with Rust build targets and release notes
  build_release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [rustfmt, clippy, audit, check, test]
    name: Build GitHub Release
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-linux-gnu
          - x86_64-unknown-linux-musl
          - armv7-unknown-linux-gnueabihf
          - armv7-unknown-linux-musleabihf
          - x86_64-apple-darwin
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: rualdi-x86_64-unknown-linux-gnu.tar.gz
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            name: rualdi-x86_64-unknown-linux-musl.tar.gz
          - target: armv7-unknown-linux-gnueabihf
            os: ubuntu-latest
            name: rualdi-armv7-unknown-linux-gnueabihf.tar.gz
          - target: armv7-unknown-linux-musleabihf
            os: ubuntu-latest
            name: armv7-unknown-linux-musleabihf.tar.gz
          - target: x86_64-apple-darwin
            os: macOS-latest
            name: rualdi-x86_64-apple-darwin.tar.gz
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - name: Set version
        id: tag
        run: echo ::set-output name=tag::${GITHUB_REF#refs/*/}
        
      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
          target: ${{ matrix.target }}

      - name: Install musl tools
        if: contains(matrix.target, 'musl')
        run: sudo apt-get install -y musl-tools

      - name: Build x86_64 target
        if: startsWith(matrix.target, 'x86_64')
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release --target ${{ matrix.target }}
      
      - name: Build armv7 target
        if: startsWith(matrix.target, 'armv7')
        uses: actions-rs/cargo@v1
        with:
          use-cross: true
          command: build
          args: --release --target ${{ matrix.target }}

      - name: Prepare build artifacts [-nix]
        run: |
          cd target/${{ matrix.target }}/release
          strip rualdi
          tar czvf ../../../${{ matrix.name }} rualdi
          cd -

      - name: Build deb x86_64 package
        if: startsWith(matrix.target, 'x86_64-unknown-linux')
        uses: ebbflow-io/cargo-deb-amd64-ubuntu@1.0
        with:
          cmd: cargo deb --target=${{ matrix.target }} -p rualdi --deb-version ${{ steps.tag.outputs.tag }} -o rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.deb

      - name: Build deb armv7 package
        if: startsWith(matrix.target, 'armv7-unknown-linux')
        uses: ebbflow-io/cargo-deb-armv7-debian@1.0
        with:
          cmd: cargo deb --target=${{ matrix.target }} -p rualdi --deb-version ${{ steps.tag.outputs.tag }} -o rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.deb

      # - name: Build rpm package
      #   if: startsWith(matrix.target, 'x86_64-unknown-linux')
      #   uses: ebbflow-io/cargo-rpm-amd64-fedora@1.0
      #   with:
      #     cmd: cd rualdi && cargo rpm --target=${{ matrix.target }} -o ../rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.rpm

      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}

      - name: Upload deb artifact
        if: contains(matrix.target, 'linux')
        uses: actions/upload-artifact@v2
        with:
          name: rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.deb
          path: rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.deb
      
      # - name: Upload rpm artifact
      #   if: startsWith(matrix.target, 'x86_64-unknown-linux')
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.rpm
      #     path: rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.rpm

      - name: Generate release notes
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: |
          # Temporary fix for https://github.com/actions/setup-go/issues/14
          export PATH=$PATH:$(go env GOPATH)/bin
          go get -u github.com/git-chglog/git-chglog/cmd/git-chglog
          git-chglog -c .github/chglog/release.yml $(git describe --tags) > RELEASE.md

      - name: Create GitHub release
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ${{ matrix.name }}
            rualdi_${{ steps.tag.outputs.tag }}_${{ matrix.target }}.deb
          body_path: RELEASE.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub release
        if: matrix.target != 'x86_64-unknown-linux-gnu'
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ matrix.name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

